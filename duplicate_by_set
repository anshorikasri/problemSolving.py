class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        my_set = set()
        # kalau kita mau gunakan set, maka jangan pakai kurung kurawal kosong
        # menggunakan kurung {} tidak akan dianggap sebagai set,
        # malahan penggunaan kurung {} akan dianggap dictionary kosong
        for n in nums:
            if n in my_set:
                return True
            my_set.add(n)
        return False
